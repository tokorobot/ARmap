# QRコード対応表

**この表は掲示物の台帳です。QRを印刷・掲示したら必ず更新してください。**

---

## 公開URL（確定・変更不可）

```
https://nobeko-map.pages.dev/
```

- 確定日: 2026-09-09
- ホスティング: Cloudflare Pages（GitHub連携・`main` ブランチを自動ビルド）
- リポジトリ: https://github.com/junpei-dev/ARmap （旧 tokorobot/ARmap から移動）

> **警告**: QRコードにはこのアドレスが焼き込まれます。
> 掲示後にドメインを変更すると、掲示済みのQRがすべて使えなくなり、貼り替えが必要になります。
> 印刷は必ず上記の公開URLで開いてから行ってください（`#/qr` の画面に警告が出ます）。

## QRのURL形式

```
https://nobeko-map.pages.dev/#/at/<ノードID>
```

ノードIDは `campus.json` の `nodes[].id`。**一度掲示したIDは絶対に変更しない。**
ラベル（表示名）は自由に変えてよいが、IDは変えないこと。

---

## 掲示予定・掲示状況

| ID | 地点 | 掲示場所 | facing実測 | 印刷 | 掲示日 |
|---|---|---|---|---|---|
| n001 | 事務室前 | 事務室窓口の前（現地で決定） | 未 | 未 | — |
| n003 | 職員室前 | 職員室入口の脇（現地で決定） | 未 | 未 | — |
| n006 | 保健室前 | 保健室入口の脇（現地で決定） | 未 | 未 | — |

### 掲示予定（現地確認後に追加）

| 地点 | 理由 |
|---|---|
| 階段（接続部） | 階を移動すると現在地が不明確になるため |
| 階段（ホール脇） | 同上 |
| 図書館入口 | 動線の確定後 |

---

## 記録の手順

1. 現地で掲示場所を決め、`campus.json` の `qr.place` を更新する
2. その場で QRを正面に見た向き（`qr.facing`）を実測して更新する
3. 公開URLで `#/qr` を開いて印刷する
4. 掲示したら、この表の「印刷」「掲示日」を埋める

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-09-09 | 公開URLを `nobeko-map.pages.dev` に確定 |
